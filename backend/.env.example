# Copy this file to backend/.env and replace the placeholders.
# backend/.env is ignored by Git and loaded automatically by local Go commands.
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-characters
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=baselium
DB_SSLMODE=disable
PORT=8080
